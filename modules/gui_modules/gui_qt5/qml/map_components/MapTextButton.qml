//MapTextButton.qml
// A simple button with text used on the map screen.
// At the moment, the only real difference
// is a different normalColor from normal MTextButton.

import QtQuick 2.0
import "../modrana_components"

MTextButton {
    normalColor : "grey"
}